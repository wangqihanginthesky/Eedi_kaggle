All the synthetic data generation is done using GPT-4o Mini. You can directly download the generated results.

You need to install kagglehub in advance.
```
pip install kagglehub
```
Then you can
```
python download.py
```
